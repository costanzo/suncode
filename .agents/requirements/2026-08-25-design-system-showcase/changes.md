# Changes

## Source

- Added `design/index.html` as the unified desktop-first review entry point.
- Added `design/dark.html` and `design/light.html` as complete theme review pages.
- Added shared `design/tokens.css`.
- Added `design/README.md` and `design/assets/README.md`.
- Added the central brand and icon asset catalog.

## Contracts and generated artifacts

No protocol or generated artifact changed.

## Configuration and persistence

No configuration or persistence changed.

## Tests

Static structure, asset references, and repository diff checks are covered by the verification commands in `test-plan.md`.

## Documentation

`DESIGN.md` now points to the unified entry point and theme review pages, and defines their authority for future client styling.
