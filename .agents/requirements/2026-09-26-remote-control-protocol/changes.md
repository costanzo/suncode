# Changes

## Source

- Added the normative mobile-facing remote-control contract directory.

## Contracts and generated artifacts

- Added `contracts/remote-control/http.openapi.yaml`.
- Added `contracts/remote-control/websocket.asyncapi.yaml`.
- Added the protocol README and lifecycle notes.

## Configuration and persistence

- No runtime or persistence changes.

## Tests

- YAML parse and focused contract structure checks are run as documented in `test-plan.md`.

## Documentation

- Linked the remote-control contract from `contracts/README.md`.
