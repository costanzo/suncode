# Rust core structure

Status: Complete

The large agent implementation is split into focused modules under `agent/crates/core/src/agent/`, and bootstrap configuration is colocated under `agent/crates/config/`.
