# Changes

- Converted `core/src/agent.rs` into the `core/src/agent/` module directory with focused includes.
- Moved the `suncode-config` crate from repository root `config/` to `agent/crates/config/`.
- Updated Cargo workspace membership and dependency paths.
