# Changes

- Added Rust-owned session-image persistence with configurable global image storage.
- Added SQLite `session_image` plus typed store and SDK methods for list/add/remove flows.
- Updated Avalonia composer attachments to upload immediately, restore from the selected session, support clipboard image paste, and remove persisted placeholders.
- Added Settings UI for the global image directory.
