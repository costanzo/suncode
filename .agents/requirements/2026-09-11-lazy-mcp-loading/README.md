# Lazy MCP loading

Status: implemented

This delivery moves project-scoped MCP startup out of project selection and into the opened Workspace window. The Workspace footer reports background startup progress until all enabled servers are connected or terminally failed.
