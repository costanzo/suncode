# Test plan

- Rust workspace and SDK compile checks.
- C ABI compile and ABI version test.
- C# desktop build.
- SDK focused test for initial progress with no enabled MCP servers.
- Design-system build and diff checks.
