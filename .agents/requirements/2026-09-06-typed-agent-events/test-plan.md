# Test Plan

- Verify enum variants map to current dotted SDK event names.
- Verify typed payload serialization preserves current field names and null handling.
- Run core unit tests and complete agent workspace tests.
- Run Clippy with warnings denied and `git diff --check`.
