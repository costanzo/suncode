# Changes

- Added root contributor guidance.
- Added the `.agents` knowledge-base index, rules, product overview, and decision index.
- Moved the approved foundational design to `.agents/ARCHITECTURE.md`.
- Migrated the completed Git-ignore plan into a full dated requirement record.
- Added reusable requirement templates and empty-state feature/spec indexes.
- Removed obsolete runtime artifacts and tool-specific documentation paths.
- Updated `.gitignore` for current local tool state.