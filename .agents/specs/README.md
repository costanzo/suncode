# Technical Specifications

This directory records current implementation facts and contracts, such as APIs, protocol behavior, database schemas, security rules, background jobs, and third-party integrations.

The approved target architecture remains in `../ARCHITECTURE.md`. Add specifications here as corresponding subsystems are designed and implemented. Canonical executable contracts and generated source must use the repository paths defined by the architecture.
