[package]
name = "suncode-common"
version = "0.1.0"
edition = "2021"

[lib]
name = "suncode_common"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
