<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework Condition="!$([MSBuild]::IsOSPlatform('Windows')) And '$(SunCodeForceWindowsTarget)' != 'true'">net10.0</TargetFramework>
    <TargetFramework Condition="$([MSBuild]::IsOSPlatform('Windows')) Or '$(SunCodeForceWindowsTarget)' == 'true'">net10.0-windows10.0.17763.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyTitle>SunCode</AssemblyTitle>
    <AssemblyName>SunCode</AssemblyName>
    <Product>SunCode</Product>
    <Version>0.0.1</Version>
    <AssemblyVersion>0.0.1.0</AssemblyVersion>
    <FileVersion>0.0.1.0</FileVersion>
    <InformationalVersion>0.0.1</InformationalVersion>
    <AvaloniaVersion>12.1.1</AvaloniaVersion>
    <AvaloniaUseCompiledBindingsByDefault>true</AvaloniaUseCompiledBindingsByDefault>
    <ApplicationManifest Condition="$([MSBuild]::IsOSPlatform('Windows'))">app.manifest</ApplicationManifest>
    <BrowserRuntimeTarget Condition="'$(RuntimeIdentifier)' == 'osx-arm64'">darwin-arm64</BrowserRuntimeTarget>
    <BrowserRuntimeTarget Condition="'$(RuntimeIdentifier)' == 'win-x64'">win32-x64</BrowserRuntimeTarget>
    <BrowserRuntimeTarget Condition="'$(RuntimeIdentifier)' == 'linux-x64'">linux-x64</BrowserRuntimeTarget>
    <BrowserRuntimeSource Condition="'$(BrowserRuntimeTarget)' != ''">$(MSBuildProjectDirectory)/../../artifacts/browser-runtime/$(BrowserRuntimeTarget)</BrowserRuntimeSource>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="AvaloniaUI.DiagnosticsSupport" Version="2.2.3" />
    <ProjectReference Include="../../sdks/csharp/SunCode.Sdk.csproj" />
    <PackageReference Include="Avalonia" Version="$(AvaloniaVersion)" />
    <PackageReference Include="Avalonia.Desktop" Version="$(AvaloniaVersion)" />
    <PackageReference Include="Avalonia.Fonts.Inter" Version="$(AvaloniaVersion)" />
    <PackageReference Include="Avalonia.AvaloniaEdit" Version="12.0.0" />
    <PackageReference Include="AvaloniaEdit.TextMate" Version="12.0.0" />
    <PackageReference Include="Svg.Controls.Skia.Avalonia" Version="12.0.0.15" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="$(AvaloniaVersion)" />
    <PackageReference Include="LiveMarkdown.Avalonia" Version="2.4.3" />
    <PackageReference Include="Microsoft.Toolkit.Uwp.Notifications" Version="7.1.3" />
    <PackageReference Include="System.Drawing.Common" Version="10.0.0"
                      Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'windows'" />
    <PackageReference Include="Tmds.DBus.Protocol" Version="0.95.1" />
  </ItemGroup>

  <Target Name="EnsureBrowserRuntimePresent" BeforeTargets="Publish"
          Condition="'$(BrowserRuntimeTarget)' != ''">
    <Error Condition="!Exists('$(BrowserRuntimeSource)/runtime-manifest.json')"
           Text="The packaged Browser Use runtime for $(BrowserRuntimeTarget) is missing. Build and verify $(BrowserRuntimeSource) before publishing." />
  </Target>

  <Target Name="CopyWindowsBrowserRuntime" AfterTargets="Publish"
          Condition="$([MSBuild]::IsOSPlatform('Windows')) And '$(BrowserRuntimeTarget)' == 'win32-x64' And Exists('$(BrowserRuntimeSource)')">
    <RemoveDir Directories="$(PublishDir)browser-runtime" />
    <Exec Command="cmd /c &quot;robocopy &quot;&quot;$(BrowserRuntimeSource)&quot;&quot; &quot;&quot;$(PublishDir)browser-runtime&quot;&quot; /E /NFL /NDL /NJH /NJS /NP &amp; IF ERRORLEVEL 8 exit /B 8 &amp; exit /B 0&quot;" />
  </Target>

  <Target Name="CopyLinuxBrowserRuntime" AfterTargets="Publish"
          Condition="$([MSBuild]::IsOSPlatform('Linux')) And '$(BrowserRuntimeTarget)' == 'linux-x64' And Exists('$(BrowserRuntimeSource)')">
    <RemoveDir Directories="$(PublishDir)browser-runtime" />
    <Exec Command="/bin/cp -a &quot;$(BrowserRuntimeSource)&quot; &quot;$(PublishDir)browser-runtime&quot;" />
  </Target>

  <ItemGroup>
    <Compile Remove="tests/**/*.cs" />
    <AvaloniaResource Include="Assets/**" />
    <Content Include="Assets/logo/suncode-logo-1024.png" CopyToOutputDirectory="PreserveNewest" Link="suncode-logo-1024.png" />
    <Content Include="packaging/linux/dev.suncode.desktop.desktop" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>


  <Target Name="CreateMacOSAppBundle" AfterTargets="Publish" Condition="$([MSBuild]::IsOSPlatform('OSX'))">
    <PropertyGroup>
      <SunCodeAppBundle>$(PublishDir)SunCode.app</SunCodeAppBundle>
    </PropertyGroup>
    <MakeDir Directories="$(SunCodeAppBundle)/Contents/MacOS;$(SunCodeAppBundle)/Contents/Resources" />
    <ItemGroup>
      <SunCodePublishedFiles Include="$(PublishDir)*" Exclude="$(SunCodeAppBundle)/**" />
    </ItemGroup>
    <Copy SourceFiles="@(SunCodePublishedFiles)" DestinationFolder="$(SunCodeAppBundle)/Contents/MacOS/%(RecursiveDir)" />
    <Copy SourceFiles="Info.plist" DestinationFolder="$(SunCodeAppBundle)/Contents" />
    <Copy SourceFiles="Assets/suncode-desktop.icns" DestinationFolder="$(SunCodeAppBundle)/Contents/Resources" />
    <Exec Command="/usr/bin/xcrun swiftc -parse-as-library -emit-library -o &quot;$(SunCodeAppBundle)/Contents/MacOS/libsuncode_notifications.dylib&quot; &quot;Native/macOS/SunCodeNotifications.swift&quot;" />
    <RemoveDir Directories="$(SunCodeAppBundle)/Contents/Resources/browser-runtime"
               Condition="'$(BrowserRuntimeTarget)' == 'darwin-arm64' And Exists('$(BrowserRuntimeSource)')" />
    <Exec Command="/usr/bin/ditto &quot;$(BrowserRuntimeSource)&quot; &quot;$(SunCodeAppBundle)/Contents/Resources/browser-runtime&quot;"
          Condition="'$(BrowserRuntimeTarget)' == 'darwin-arm64' And Exists('$(BrowserRuntimeSource)')" />
  </Target>
</Project>
