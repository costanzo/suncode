# Fonts

The review pages use installed `Noto Sans` / `Noto Sans CJK SC` and `JetBrains Mono` with documented fallbacks. Add licensed font files here only when the distribution plan is approved; do not commit system-font copies by default.
