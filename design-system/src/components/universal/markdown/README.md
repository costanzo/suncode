# Markdown

`MarkdownSpecimen.jsx` is a static semantic-HTML specimen of the expected post-render Markdown surface. It intentionally has no Markdown parser or fixture dependency. `Markdown.css` owns every Markdown-specific visual rule; global review styles must not define `.markdown*` selectors.
