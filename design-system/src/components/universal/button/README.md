# Button

`ButtonSpecimens.jsx` owns shared button variants, sizes, states, and authority-action examples. Consumers import it through this directory's `index.js`; the universal page only places it in the Actions section.
