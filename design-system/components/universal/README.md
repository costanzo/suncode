# Universal Components

This layer owns cross-platform interaction primitives and their review states. The static theme inventories under `themes/` are the current visual contract; individual component folders are reserved for reusable examples and implementation notes.

The Avalonia client remains the runtime owner of presentation. This directory must not become a second production component library.
