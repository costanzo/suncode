# Universal Components

This layer owns cross-platform interaction primitives and their review states. `react/UniversalComponentsPage.jsx` is the interactive visual contract for both themes; individual component folders hold reusable implementation notes and component-specific guidance.

The Avalonia client remains the runtime owner of presentation. This directory must not become a second production component library.
